export enum StorageEnum {
  User = 'user',
  Token = 'token',
}

export enum AuthApi {
  Login = '/auth/login',
  Register = '/auth/register',
}

export enum UserApi {
  UpdateUser = '/user',
  updatePassword = '/user/password',
  updateEmail = '/user/email'
}
